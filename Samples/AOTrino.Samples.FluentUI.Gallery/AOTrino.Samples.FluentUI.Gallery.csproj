<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<OutputType>WinExe</OutputType>
		<PublishAot>true</PublishAot>
		<ApplicationManifest>app.manifest</ApplicationManifest>
		<RootNamespace>AOTrino.Samples.FluentUI.Gallery</RootNamespace>

		<!-- opt in to AOTrino's WebRoot embedding (see Directory.Build.targets) -->
		<AOTrinoEmbedWebRoot>true</AOTrinoEmbedWebRoot>

		<!-- ...and build WebRoot with npm first: WebRoot\dist is generated by Vite, not hand-written -->
		<AOTrinoNpmBuild>true</AOTrinoNpmBuild>

		<Title>AOTrino Samples - FluentUI - Gallery</Title>
		<AssemblyTitle>AOTrino Samples - FluentUI - Gallery</AssemblyTitle>
	</PropertyGroup>

	<ItemGroup>
		<Using Include="AOTrino" />
		<Using Include="AOTrino.Bridge" />
		<Using Include="System" />
		<Using Include="System.Diagnostics" />
		<Using Include="System.Globalization" />
		<Using Include="System.Reflection" />
		<Using Include="System.Runtime.InteropServices" />
		<Using Include="System.Collections.Generic" />
		<Using Include="System.Text.Json" />
		<Using Include="System.Text.Json.Serialization" />
		<Using Include="System.Threading.Tasks" />
		<Using Include="DirectN" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\..\AOTrino\AOTrino.csproj" />
	</ItemGroup>

	<!--
	  the declared package versions, baked in as assembly metadata so the Packages page can show what the project
	  asked for next to what actually loaded. DirectNAotVersion / WebView2AotVersion come from Directory.Build.targets.
	-->
	<ItemGroup>
		<AssemblyMetadata Include="AOTrino" Value="$(Version)" />
		<AssemblyMetadata Include="DirectNAot" Value="$(DirectNAotVersion)" />
		<AssemblyMetadata Include="DirectNAot.Extensions" Value="$(DirectNAotVersion)" />
		<AssemblyMetadata Include="WebView2Aot" Value="$(WebView2AotVersion)" />
	</ItemGroup>


</Project>
